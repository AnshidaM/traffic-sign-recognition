version: "3.9"

services:
  backend:
    build: ./backend
    container_name: traffic-backend
    ports:
      - "8000:8000"

  frontend:
    build: .
    container_name: traffic-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
